.intel_syntax noprefix
.globl main
main:
	push rbp
	mov rbp, rsp
	sub rsp, 208

	push 88

	pop rax
	mov rsp, rbp
	pop rbp
	ret

	pop rax

	push 77

	pop rax
	mov rsp, rbp
	pop rbp
	ret

	pop rax

	push 77

	pop rax
	mov rsp, rbp
	pop rbp
	ret

	pop rax

	push 7
	
	push 2

	pop rdi
	pop rax
	cmp rax, rdi
	sete al
	movzx rax, al
	push rax

	pop rax
	cmp rax, 0
	je .Lelse000
	push 88

	pop rax
	mov rsp, rbp
	pop rbp
	ret

	jmp .Lend000
.Lelse000:
	push 77

	pop rax
	mov rsp, rbp
	pop rbp
	ret

.Lend000:
	pop rax

	mov rsp, rbp
	pop rbp
	ret

